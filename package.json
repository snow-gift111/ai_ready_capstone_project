{
  "name": "jira-task-management-playwright",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright TypeScript automation for Jira Task Management",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3"
  }
}

