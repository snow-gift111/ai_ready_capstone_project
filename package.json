{
  "name": "jira-task-management-playwright",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright + TypeScript POM automation for Jira Task Management",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.0",
    "@types/node": "^20.14.9",
    "typescript": "^5.5.4"
  }
}

